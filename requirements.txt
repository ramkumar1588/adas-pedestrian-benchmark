# ── Core ML / Inference ───────────────────────────────────────────────────────
torch>=2.1.0                  # install CUDA build: see README Installation
torchvision>=0.16.0
ultralytics>=8.0.0            # YOLOv8 + RT-DETR + TensorRT export

# ── Dataset & Evaluation ─────────────────────────────────────────────────────
pycocotools>=2.0.7            # mAP evaluation (Windows: pip install pycocotools-windows)
zod>=0.8.0                    # Zenseact Open Dataset SDK (metadata reading)

# ── Interpretability ─────────────────────────────────────────────────────────
pytorch-grad-cam>=1.5.0       # GradCAM for YOLOv8
opencv-python>=4.9.0          # image I/O and heatmap overlay

# ── Statistics ───────────────────────────────────────────────────────────────
scipy>=1.12.0                 # ANOVA, Mann-Whitney U, linear regression
statsmodels>=0.14.0           # two-way ANOVA with interaction term
pandas>=2.1.0                 # data manipulation

# ── Utilities ────────────────────────────────────────────────────────────────
pyyaml>=6.0                   # config.yaml parsing
tqdm>=4.66.0                  # progress bars
numpy>=1.26.0
